{
  "name": "cloud-bridge",
  "version": "1.0.0",
  "description": "Simple webserver that bridges tp-link power plugs on the local network to the web",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sumeet Parmar",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.1",
    "tplink-smarthome-api": "^4.2.0"
  }
}
